function repeatStringNumTimes(str, num) {
  let s="";
  if(num<1)
    return s;
  else if(num==1)
    return str;
  else{
    while(num!=0){
      s+=str;
      num--;
    }
  }
  return s;
}

repeatStringNumTimes("abc", 3);
